<% flash.each do |messages_type, message_content| %>
  <div class="ui message">
    <i class="close icon"></i>
      <div class="header">
        <%= message_content %>
      </div>
  </div>
  <% end %>